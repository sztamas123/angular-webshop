import { PaymentInformation } from './payment-information';

describe('PaymentInformation', () => {
  it('should create an instance', () => {
    expect(new PaymentInformation()).toBeTruthy();
  });
});
